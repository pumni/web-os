/**
 * Shared feature modules for the Pumni Web OS monorepo.
 *
 * Re-export the public API of each cross-app feature from this barrel as the
 * features are extracted out of `apps/web/src/features`. Keeping them here lets
 * multiple apps consume the same feature logic.
 */

export {};
