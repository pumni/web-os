# Supabase migrations

Root `AGENTS.md` applies. For work in this subtree, read
`docs/conventions/supabase-security.md` and activate
`.agents/skills/supabase-migration/SKILL.md`.

Committed SQL is migration history: add a new migration for schema, policy, or
function changes. Use the focused migration/RLS tests and the generated types
workflow named by the skill.
