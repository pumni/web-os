# Pumni Web OS

Follow the repository rules in `AGENTS.md`. For Next.js app code, also follow
`apps/web/AGENTS.md`. Run `bun run ai:eval` before reporting done on security-sensitive diffs
(Supabase migrations, auth, RLS, server actions).
