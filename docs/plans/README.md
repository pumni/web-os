# Active plans

This directory contains only implementation plans that describe current work.
Active plans are subordinate to repository source, security boundaries, CI, and
the applicable `AGENTS.md` files.

Completed plans are removed from the working tree after acceptance; Git history
is the archive. Do not create a permanent completed-plan directory.

## Current

- [`context-layer-v3/`](./context-layer-v3/) — active Context Layer v3 handoff;
  remove only after implementation and CI acceptance are complete.
- [`docs-knowledge-prune/`](./docs-knowledge-prune/) — strict documentation/ADR
  cleanup; make the working tree contain only current operational knowledge.
