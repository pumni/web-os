# PLAN_A baseline — Tool Support Matrix ownership tally

Status: pre-flight evidence for Workstream A of `context-layer/improvements-2026-07-08`.
Created by refactor-plan skill. Tree-state evidence only; not a decision (no ADR).

## What the repo actually has (capability → mechanism → owning path)

| # | Capability | Mechanism | Owning path | Tools that consume it |
|---|---|---|---|---|
| 1 | Static entry contract | `AGENTS.md` (P0–P6, security, priorities) | `AGENTS.md` | All agents that read AGENTS.md (Codex CLI, Gemini CLI, Cursor, Copilot, opencode). Claude Code via `CLAUDE.md` import. |
| 2 | Claude Code shim | `@AGENTS.md` pointer + 2 Claude-only lines | `CLAUDE.md` | Claude Code only |
| 3 | Agentic handshake map | link map, ADR-0022 | `llms.txt` | Agents that `fetch`/read it (not auto-crawled — §6.5 of the Kim chỉ nam) |
| 4 | Need→doc router | single hub after AGENTS.md | `docs/ai/index.md` | All agents that reach it; not agent-auto-loaded except via AGENTS.md instruction |
| 5 | Long-term memory | settled-decision log, hybrid memory model | `docs/ai/MEMORY.md` | All agents (read manually). Not in `docs/ai/index.md` today (mismatch row) |
| 6 | Path-scoped static rules | `.mdc`-style frontmatter with `globs:` | `.claude/rules/nextjs-async-apis.md`, `.claude/rules/nextjs-cache-components.md` | Claude Code (native path rules). Cursor `.mdc` would also read similar frontmatter. |
| 7 | Skill discovery (canonical) | `SKILL.md` + YAML frontmatter, agentskills.io spec | `.agents/skills/<name>/SKILL.md` | Any Skill-aware agent (Claude Code, Codex CLI via `codex skills install`, others per spec) |
| 8 | Skill shim (generated) | pointer shim, name+description only | `.claude/skills/<name>/SKILL.md` | Claude Code only. Generated by `bun run ai:skills:sync`; never hand-edited. |
| 9 | Subagent domain reviewers | `.md` agent definition `tools: Read, Grep, Glob` | `.claude/agents/supabase-rls-reviewer.md`, `.claude/agents/watch-sync-reviewer.md` | Claude Code only. Dispatched optionally from `review-gate.md`. |
| 10 | Hooks (lifecycle enforcement) | SessionStart/PostToolUse/Stop → `bun` script | `.claude/hooks/context-drift-notice.mjs`, `.claude/hooks/format-edited-file.mjs`, `.claude/hooks/ai-context-stop-gate.mjs`, registered in `.claude/settings.json` | Claude Code only. Non-Claude agents get NO `ai:check` enforcement before "done". |
| 11 | MCP servers | local dev aids, opt-in, disabled by default | `.mcp.json` + `docs/ai/mcp.md` | Claude Code, Codex, opencode (any MCP client). Not CI. |
| 12 | Manifest of required files/build scripts/size budgets | `scripts/ai-context.manifest.json` consumed by `scripts/check-ai-context.mjs` | `scripts/*.mjs` + manifest | Tooling (CI), not agents directly. |
| 13 | Validation gate (always-on advisory) | `bun run ai:check`, `bun run ai:eval`, `bun run ai:metrics` | `package.json` scripts | Human + Claude Code Stop hook. Non-Claude agents must run manually. |

## What `docs/ai/index.md` actually surfaces today (2026-07-08, baseline)

Reading `docs/ai/index.md` end-to-end, a non-Claude agent would learn:

| It would learn | It would NOT learn (gap → mismatch) |
|---|---|
| Conventions table (10 rows) | Hooks are Claude-only → no enforcement if not Claude Code |
| Next.js 16 rules (2 rows, auto-loaded by glob) | Path-scoped rules' `globs:` are Claude Code–specific |
| Reference (5 rows: shell policy, domain glossary, golden examples, common mistakes, MCP) | **MEMORY.md is not listed** (mismatch #1) |
| Skills (18 rows); a note: "Claude Code auto-surfaces these by description via thin shims"; pointer to `.agents/skills/README.md` authoring standard | Whether `.claude/skills/<name>/SKILL.md` shims are generated or hand-written; that non-Claude agents should read `.agents/skills` only |
| Workflows (1 row: review-gate) | That `review-gate.md` can optionally dispatch Claude-Code-only subagents |
| That MCP servers exist (`docs/ai/mcp.md`) | That MCP servers are opt-in + disabled by default + are NOT CI deps |
| Verification pointer to `agent-command-policy.md#validation` | That the Stop hook is Claude-only → gate becomes advisory for other agents |

## Mismatch tally (rows in repo missing from `docs/ai/index.md`)

| ID | Capability path | Index says | Truth | Severity |
|---|---|---|---|---|
| M-1 | `docs/ai/MEMORY.md` | not listed in `## Reference (load on demand)` | exists, is the long-term settled-decision log, `llms.txt` surfaces it | High — agents bypassing `llms.txt` won't discover memory |
| M-2 | `.claude/hooks/*.mjs` | not mentioned anywhere | 3 hooks enforce `ai:check` on context edits; only Claude Code runs them | High — non-Claude agents believe no enforcement gap exists |
| M-3 | `.claude/rules/*.md` `globs:` | only "Next.js 16 (auto-loaded by glob; read directly when editing app code)" as a section header | globs are Claude-Code-style; Cursor `.mdc` would also use globs but Codex/Gemini/opencode do not auto-load | Medium — listed separately as "auto-loaded" might mislead a non-Claude agent |
| M-4 | `.claude/agents/*-reviewer.md` | not mentioned | 2 read-only subagents, dispatched from `review-gate.md` Verification section | Medium — `review-gate.md` does mention them, but index has no contract that they are Claude-only |
| M-5 | `.claude/skills/<name>/SKILL.md` | "Claude Code auto-surfaces these by description via thin shims in `.claude/skills/<name>/SKILL.md`; the canonical, tool-agnostic content lives in `.agents/skills` (the shims just point back to it)." | Accurate, but missing: "shims are **generated** by `bun run ai:skills:sync`; do not hand-edit." | Low — already partial truth |
| M-6 | `.mcp.json` + `docs/ai/mcp.md` | row says "MCP servers (next-devtools runtime + postgres schema, optional)" | accurate, but missing "disabled by default locally; never CI" | Low |

## Severity → workstream mapping

- **M-1 (MEMORY)** → Step A2 adds the missing Reference row.
- **M-2, M-3, M-4, M-5, M-6** → Step A1 adds a single new `## Tool Support Matrix` section with one row per capability × tool support. Avoids per-row duplication across the file; one consolidated matrix is cheaper than five scattered footnotes (size budget: `docs/ai/index.md` is not in `sizeBudgets` today, but A1 should keep file compact per the table-compactness gate).

## Baseline metric (to feed A4)

After A1 + A2 land, the new metric `toolMatrixMismatches` should read `0`. Before A1, an inline check (parse index.md for required keywords) should report `\u2265 2` mismatches. Verified by A4.

## Non-goals for A0

- No edit to `docs/ai/index.md` in this step.
- No metric code change in this step.
- No edit to `llms.txt` in this step.

## Verification recorded

- File created: `docs/plans/PLAN_A_baseline_2026-07-08.md` (excluded from `check-ai-context.mjs` link checks per `:96`).
- Working tree: only this new file added.
- Gate re-run not required (this file is in `docs/plans/` which is excluded).
